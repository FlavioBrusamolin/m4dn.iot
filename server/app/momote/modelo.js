/*var mongoose = require('mongoose');
var Schema = mongoose.Schema;

var momoteSchema = new Schema({
    ip: {
        type: String,
        required: true
    }
});

var Momote = mongoose.model('momotes', momoteSchema);

module.exports = Momote;*/ 